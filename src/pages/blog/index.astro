---
import { SITE_LOCALE, withBase } from '../../consts';

const homeUrl = withBase();
---

<!doctype html>
<html lang={SITE_LOCALE}>
	<head>
		<meta charset="utf-8" />
		<meta http-equiv="refresh" content={`0;url=${homeUrl}`} />
		<script is:inline define:vars={{ homeUrl }}>
			window.location.replace(homeUrl);
		</script>
	</head>
	<body>
		<p>正在跳转到首页...</p>
	</body>
</html>
